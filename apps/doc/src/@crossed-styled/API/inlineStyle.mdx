import { Source } from '@storybook/blocks';

# inlineStyle

inlineStyle allows you to create styling directly in the rendering of components

<Source
  language="tsx"
  code={`export const Toto = () => <div {...inlineStyle(() => ({ base: { backgroundColor: "black" } })).className()} />`}
/>
