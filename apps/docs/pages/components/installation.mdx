import { Tabs } from 'components/Tabs';

# Installation

<Tabs items={["pnpm", "npm", "yarn"]}>
  <Tabs.Tab>

    ```sh
    pnpm i @crossed/ui
    ```

  </Tabs.Tab>
  <Tabs.Tab>

    ```sh
    npm i @crossed/ui
    ```

  </Tabs.Tab>
  <Tabs.Tab>
  
    ```sh
    yarn add @crossed/ui
    ```
  
  </Tabs.Tab>
</Tabs>
