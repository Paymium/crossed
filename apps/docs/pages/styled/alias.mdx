# Alias

## `merge`

Default function to use if you're using the default Tailwind config or are close enough to the default config  
(an alias of [twMerge](https://github.com/dcastil/tailwind-merge/blob/v1.14.0/docs/api-reference.md#twmerge))
```typescript
function twMerge(
    ...classLists: Array<string | undefined | null | false | 0 | typeof classLists>
): string
```

## `tw`
An alias of [tw](https://github.com/jaredh159/tailwind-react-native-classnames)
