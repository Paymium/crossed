import { CreateDropdownSimpleDemo } from '@crossed/demo';
import { CodeDemo } from 'components/CodeDemo';
import { getStaticDemo } from 'components/getStaticDemo';
import { Tabs } from 'components/Tabs';

export const getStaticProps = async (e) => {
  return {
    props: {
      ssg: {
        simple: (await getStaticDemo('primitive/createDropdown/simple')(e)).props.ssg.code,
      }
    }
  }
};

# createDropdown

<Tabs items={['Demo']}>
  <Tabs.Tab>
    ## simple
    <br />
    <CodeDemo Demo={CreateDropdownSimpleDemo} name="simple" />

  </Tabs.Tab>
</Tabs>
