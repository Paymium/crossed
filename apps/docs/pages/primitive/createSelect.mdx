import { CreateSelectSimpleDemo } from '@crossed/demo';
import { CodeDemo } from 'components/CodeDemo';
import { getStaticDemo } from 'components/getStaticDemo';
import { Tabs } from 'components/Tabs';

export const getStaticProps = async (e) => {
  return {
    props: {
      ssg: {
        simple: (await getStaticDemo('primitive/createSelect/simple')(e)).props.ssg.code,
      }
    }
  }
};

# createSelect

<Tabs items={['Demo']}>
  <Tabs.Tab>
    ## simple
    <br />
    <CodeDemo Demo={CreateSelectSimpleDemo} name="simple" />

  </Tabs.Tab>
</Tabs>
