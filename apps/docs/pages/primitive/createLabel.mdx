import { CreateLabelSimpleNativeDemo } from '@crossed/demo';
import { CodeDemo } from 'components/CodeDemo';
import { getStaticDemo } from 'components/getStaticDemo';
import { Tabs } from 'components/Tabs';
import { Table } from 'components/Table';

export const getStaticProps = async (e) => {
  return {
    props: {
      ssg: {
        simpleNative: (
          await getStaticDemo('primitive/createLabel/simple.native')(e)
        ).props.ssg.code,
      },
    },
  };
};

# createLabel

<Tabs items={['Demo', 'Anatomy', 'Props', "Hooks"]}>
  <Tabs.Tab>
    <CodeDemo Demo={CreateLabelSimpleNativeDemo} name="simpleNative" />
  </Tabs.Tab>
  <Tabs.Tab>
    ```tsx
    import {
      createLabel,
      useLabelContext,
    } from "@crossed/primitive";

    const Label = createLabel({
      Root,
      Text,
    });

    <Label>
      <Label.Text />
      <Label.Input />
    </Label>
    ```

  </Tabs.Tab>
  <Tabs.Tab>
    ```tsx
    import { createLabel } from "@crossed/primitive";

    const Label = createLabel({
      Root,
      Text,
    });
    ```

    ### Root
    The `Root` component represents the Label wrapper

    ### Text
    Represents the text of label
    <Table props={[
      ["children", "string", "", "The children should be a string for complete accessibility"],
    ]} />

  </Tabs.Tab>

  <Tabs.Tab>
    ## useLabelContext

    Retrieve the label context.

    ### Syntax

    ```typescript
    function useLabelContext(): ContextLabel {
      // Function body
    }
    ```

    ### Parameters

    None.

    ### Return Value

    An object of type `ContextLabel` with:
    <Table props={[
      ["id", "string", "", "id for complete accessibility"],
      ["inputRef", "MutableRefObject<View | undefined>", "", "the input ref"],
    ]} />

    ### Example

    ```typescript
    // Example of using the function
    const result = useButtonContext();
    console.log(result);
    ```

    ### Remarks

    None.

  </Tabs.Tab>
</Tabs>
