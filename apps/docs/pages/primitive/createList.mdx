import {
  CreateListSimpleNativeDemo,
  CreateListWithSeparatorNativeDemo,
  CreateListPressableDemo,
} from '@crossed/demo';
import { CodeDemo } from 'components/CodeDemo';
import { getStaticDemo } from 'components/getStaticDemo';
import { Tabs } from 'components/Tabs';
import { Table } from 'components/Table';

export const getStaticProps = async (e) => {
  return {
    props: {
      ssg: {
        simpleNative: (
          await getStaticDemo('primitive/createList/simple.native')(e)
        ).props.ssg.code,
        pressable: (await getStaticDemo('primitive/createList/pressable')(e))
          .props.ssg.code,
        separatorNative: (
          await getStaticDemo('primitive/createList/separator.native')(e)
        ).props.ssg.code,
      },
    },
  };
};

# createList

<Tabs items={['Demo', 'Anatomy', 'Components']}>
  <Tabs.Tab>
    ## simple
    <CodeDemo Demo={CreateListSimpleNativeDemo} name="simpleNative" />

    ## Pressable
    <CodeDemo Demo={CreateListPressableDemo} name="pressable" />

  </Tabs.Tab>
  <Tabs.Tab>
    ```tsx
    import { createList } from "@crossed/primitive";

    const List = createList({
      Root,
      Item,
      SubTitle,
      Title,
      Divider,
      Label
    });

    <List>
      <List.Label />
      <List.Divider />
      <List.Item>
        <List.Title />
        <List.Subtitle />
      </List.Item>
    </List>
    ```

  </Tabs.Tab>
  <Tabs.Tab>
    ### Root
    Composant Root de la liste

    ### Item
    Un element de la liste

    ### Title
    Titre de l'élément de la list

    ### SubTitle
    Un element de la liste

    ### Label
    Label of section in list

    ### Divider
    Divider of List
    {/* <Table props={[]} /> */}

  </Tabs.Tab>
</Tabs>
