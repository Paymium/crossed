import { CreateSheetSimpleDemo } from '@crossed/demo';
import { CodeDemo } from 'components/CodeDemo';
import { getStaticDemo } from 'components/getStaticDemo';
import { Tabs } from 'components/Tabs';

export const getStaticProps = async (e) => {
  return {
    props: {
      ssg: {
        simple: (await getStaticDemo('primitive/createSheet/simple')(e)).props.ssg.code,
      }
    }
  }
};

# createSheet

<Tabs items={['Demo']}>
  <Tabs.Tab>
    ## simple
    <br />
    <CodeDemo Demo={CreateSheetSimpleDemo} name="simple" />

  </Tabs.Tab>
</Tabs>
