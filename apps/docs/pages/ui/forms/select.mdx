import { CodeDemo } from 'components/CodeDemo';
import { Table } from 'components/Table';
import { Tabs } from 'components/Tabs';
import { getStaticDemo } from 'components/getStaticDemo';
import { SelectDemo } from '@crossed/demo';

export const getStaticProps = getStaticDemo('ui/forms/Select');

# Select

<Tabs items={["Demo", "Anatomy", "Props"]}>
    <Tabs.Tab><CodeDemo Demo={SelectDemo} actions={{ color: true, variant: ['outlined', "filled"], size: true} } /></Tabs.Tab>
    <Tabs.Tab>
    ## Simple
    ```tsx
    <Select />
    ```

    ## Advanced
    ```tsx
    <Select>
      <Select.Label />
      <Select.Trigger>
        <Select.Input />
        <Select.Icon>
            <Icon />
        </Select.Icon>
      </Select.Trigger>
      <Select.Content>
          <Select.Option />
      </Select.Content>
    </Select>
    ```
    </Tabs.Tab>
    <Tabs.Tab>
    ## Select
    <br />
    <Table props={[
      ["variant", "outline | filled", "outline", "Variant of button"],
      ["size", "xs | sm | md | lg | xl", "md", "Size of button"],
      ["color", "type Colors", "zinc", "Color of button"],
      ["value", "any", "undefined", "Value of select"],
      ["defaultValue", "any", "undefined", "Default value of select"],
      ["placeholder", "string", "undefined", "Placeholder of Select"],
      ["onChangeValue", "(value: any) => void", "undefined", "Fire when user select option"],
      ["items", "SelectOptionProps[]", "", "List of options in simple mode"],
      ["label", "string", "undefined", "Label of Select"],
    ]} />

    ## Select.Label
    Extends [Input.Label](/ui/forms/input?t=props#inputlabel) props.

    ## Select.Trigger
    Extends [Input.Content](/ui/forms/input?t=props#inputcontent) props.
  
    ## Select.Input
    Extends [Input.Input](/ui/forms/input?t=props#inputinput) props.
  
    ## Select.Icon
    Extends [Input.Icon](/ui/forms/input?t=props#inputicon) props.
  
    ## Select.Content
    Extends [Box](/ui/layout/box?t=props) props.
  
    ## Select.Option
    <br />
    <Table props={[
      ["value", "any", "undefined", "Value of option"],
      ["label", "string", "undefined", "Label of option"],
    ]} />

   </Tabs.Tab>
</Tabs>
