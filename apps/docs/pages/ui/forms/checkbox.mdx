import { CodeDemo } from 'components/CodeDemo';
import { CheckboxDemo } from '@mergeui/demo';
import { getStaticDemo } from 'components/getStaticDemo';
import { Tabs } from 'components/Tabs';
import { Table } from 'components/Table';

export const getStaticProps = getStaticDemo('ui/forms/Checkbox');

# Checkbox

<Tabs items={["Demo", "Anatomy", "Props"]}>
  <Tabs.Tab><CodeDemo Demo={CheckboxDemo} actions={{ color: true, size: true, variant: ["outlined", "filled"]}} /></Tabs.Tab>
  <Tabs.Tab>
    ## Simple
    ```tsx
    <Checkbox />
    ```

    ## Advanced
    ```tsx
      <Checkbox>
        <Checkbox.Indicator>
          <Checkbox.Icon />
        </Checkbox.Indicator>
        <Checkbox.Label />
      </Checkbox>
    ```
    </Tabs.Tab>
    <Tabs.Tab>
      ## Checkbox
      <Table
        props={[
          ["checked", "boolean", "undefined", "State of checkbox"],
          ["onChangeValue", "(value: boolean) => void", "undefined", "Fire when user toggle checkbox"],
          ["variant", "outline | filled", "outline", "Variant of button"],
          ["size", "xs | sm | md | lg | xl", "md", "Size of button"],
          ["color", "type Colors", "zinc", "Color of button"],
        ]}
      />

      ## Checkbox.Indicator
      Extends [Pressable](https://reactnative.dev/docs/pressable) props.
      <Table
        props={[
          ["checked", "boolean", "undefined", "State of checkbox"],
          ["onChangeValue", "(value: boolean) => void", "undefined", "Fire when user toggle checkbox"],
          ["variant", "outline | filled", "outline", "Variant of button"],
          ["size", "xs | sm | md | lg | xl", "md", "Size of button"],
          ["color", "type Colors", "zinc", "Color of button"],
        ]}
      />
  
      ## Checkbox.Icon
      Extends [Box](/ui/layout/box?t=props) props.

      ## Checkbox.Label
      Extends [Label.Text](/ui/forms/label?t=props#labeltext) props.

   </Tabs.Tab>
</Tabs>
