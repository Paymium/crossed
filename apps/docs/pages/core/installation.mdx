import { Tabs } from 'components/Tabs';

# Installation

<Tabs items={["pnpm", "npm", "yarn"]}>
  <Tabs.Tab>

    ```sh
    pnpm i @mergeui/core
    ```

  </Tabs.Tab>
  <Tabs.Tab>

    ```sh
    npm i @mergeui/core
    ```

  </Tabs.Tab>
  <Tabs.Tab>
  
    ```sh
    yarn add @mergeui/core
    ```
  
  </Tabs.Tab>
</Tabs>
