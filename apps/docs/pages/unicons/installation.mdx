
import { Tabs } from 'components/Tabs';

# Installation

<Tabs items={["pnpm", "npm", "yarn"]}>
  <Tabs.Tab>

    ```sh
    pnpm i @mergeui/unicons
    ```

  </Tabs.Tab>
  <Tabs.Tab>

    ```sh
    npm i @mergeui/unicons
    ```

  </Tabs.Tab>
  <Tabs.Tab>
  
    ```sh
    yarn add @mergeui/unicons
    ```
  
  </Tabs.Tab>
</Tabs>
