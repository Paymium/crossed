// import { CrossedProvider } from '@crossed/ui';
// import { config } from '../crossed.config';

export default function App({ Component, pageProps }) {
  return (
    // <CrossedProvider config={config}>
      <Component {...pageProps} />
    // </CrossedProvider>
  );
}
