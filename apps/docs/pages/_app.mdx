import "../styles/global.css"
// import { getAllDemo } from "../components/getAllDemo"

export default function App({ Component, pageProps }) {
  return (
    // <CrossedProvider config={config}>
      <Component {...pageProps} />
    // </CrossedProvider>
  );
}

// export const getInitialProps = getAllDemo;